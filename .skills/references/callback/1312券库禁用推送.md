# 1312券库禁用推送接口对接

---

> ## ⚠️ 重要：必须严格按照本文档示例代码生成
> 
> 生成代码时必须遵循以下规则：
> - **示例代码中的类名、方法名、参数类型必须完全一致**
> - **示例代码中的静态方法模式不得改为依赖注入模式**
> - **示例代码中的命名空间、文件路径必须按照说明创建**
> - **文档示例 > 项目现有风格**（即使项目中其他代码使用不同模式，也必须按本文档示例生成）

---

## 概述

本文档描述如何对接EZR开放平台的推送回调接口。推送回调与普通接口的主要区别在于：

- 推送没有现成的接口地址，需要自定义接口地址
- 推送是对接其他第三方的接口或者自定义业务处理

### 接口说明

#### 接口基础信息

| 项目 | 说明 |
|------|------|
| 接口编号 | 1312 |
| 接口名称 | 券库禁用推送 |
| 接口地址 | /api/Coupon/CoupGrpDisable |
| 协议 | HTTPS |
| 请求方式 | POST |
| 数据格式 | application/x-www-form-urlencoded |
| 字符编码 | UTF-8 |

#### 功能描述

当券库被禁用时，EZR系统会通过该接口实时推送券库禁用信息给第三方系统，用于第三方系统同步券库状态。

---

## 请求结构

### 外层请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| AppId | string | 是 | 品牌编码，由 EZR 分配 |
| Sign | string | 是 | 请求签名 |
| Timestamp | string | 是 | 请求时间戳，格式 `yyyyMMddHHmmss` |
| AppSystem | string | 是 | 系统来源标识 |
| Args | string | 是 | 业务请求参数JSON字符串 |

### 业务请求参数（Args）

Args为JSON字符串，解析后为券库禁用信息对象，包含以下字段：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| BrandId | int | 品牌ID |
| CoupGrpId | int | 券库ID |
| CoupGrpCode | string | 券库编码 |
| DisabledDate | string | 禁用时间（yyyy-MM-dd HH:mm:ss格式） |
| Platform | int | 平台类型：0=非跨平台；1=跨平台 |

### 请求示例

```json
{
    "AppId": "1001",
    "Sign": "A1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6Q7R8S9T0",
    "Timestamp": "20260323143052",
    "AppSystem": "CRM",
    "Args": "{\"BrandId\":1,\"CoupGrpId\":1001,\"CoupGrpCode\":\"COUP001\",\"DisabledDate\":\"2026-03-23 14:30:52\",\"Platform\":0}"
}
```

---

## 响应结构

### 标准响应格式

```json
{
    "Status": true,
    "StatusCode": "0",
    "Msg": "成功",
    "Sign": "响应签名",
    "Timestamp": "20260323143052",
    "Result": ""
}
```

| 参数名 | 类型 | 说明 |
|--------|------|------|
| Status | bool | 请求是否成功 |
| StatusCode | int | 状态码，200表示成功 |
| Msg | string | 响应消息 |
| Sign | string | 响应签名 |
| Timestamp | string | 响应时间戳 |
| Result | object | 业务返回数据 |

---

## 接入流程

### 1. 项目结构介绍

| 项目名称 | 描述 | 备注 |
| -------- | -------- | -------- |
| `*.ApiHost、*.Site` | 接口层 | Web API 宿主应用程序（入口点，.NET 5.0） |
| `*.ApiModel` | 实体层 | 数据模型、实体和 DTO（.NET Standard 2.0） |
| `*.Bussiness` | 业务层 | 业务逻辑层（.NET Standard 2.0） |
| `*.Intergration` | 领域层 | 用于调用其他业务grpc领域方法 |
| `*.Repository` | 数据仓储层 | 访问数据库相关的逻辑，执行增删改查（.NET Standard 2.0） |
| `*.Utils` | 工具层 | 通用帮助类,共享工具类（.NET Standard 2.0） |
| `*.Test` | 单元测试层 | 用于单元测试实例 |

### 2. 定义Controller和Action

在项目接口层，在Controllers目录下创建Controller：

```csharp
using EZP.Framework.Util.Model;
using EZP.Open.Api.Business.ThirdParty.YourModule;
using Microsoft.AspNetCore.Mvc;

namespace EZP.Open.Api.Controllers
{
    [Route("api/[controller]")]
    [ApiController]
    public class CouponController : ControllerBase
    {
        /// <summary>
        /// [EZR-API:1312] 券库禁用推送
        /// 1312 - 券库禁用推送
        /// </summary>
        [HttpPost("CoupGrpDisable")]
        public BaseApiResInfo<string> CoupGrpDisable([FromBody] BaseApiReqInfo requestObj)
        {
            return CoupGrpManager.CoupGrpDisable(requestObj);
        }
    }
}
```

### 3. 定义业务处理类

在项目业务层，在对应的业务目录下创建业务处理类：

```csharp
using System;
using EZP.Framework.Util.Model;
using EZP.Framework.Util.Serialize;
using EZP.Open.Api.Common.ThirdParty.YourModule;

namespace EZP.Open.Api.Business.ThirdParty.YourModule
{
    /// <summary>
    /// 券库禁用推送业务处理
    /// </summary>
    public class CoupGrpManager
    {
        /// <summary>
        /// 券库禁用推送处理
        /// </summary>
        public static BaseApiResInfo<string> CoupGrpDisable(BaseApiReqInfo requestObj)
        {
            try
            {
                // 解析请求参数（Args为JSON字符串）
                var request = FastJsonSerializer.FromJson<CouponGrpDisableInfo>(requestObj.Args);
                if (request == null)
                {
                    return new BaseApiResInfo<string>
                    {
                        Status = false,
                        StatusCode = 1001,
                        Msg = "请求参数不正确",
                        Timestamp = DateTime.Now.ToString("yyyyMMddHHmmss")
                    };
                }

                // TODO: 实现具体的业务处理逻辑
                // 例如：同步券库禁用状态到第三方系统

                return new BaseApiResInfo<string>
                {
                    Status = true,
                    StatusCode = 200,
                    Msg = "成功",
                    Timestamp = DateTime.Now.ToString("yyyyMMddHHmmss"),
                    Result = ""
                };
            }
            catch (Exception ex)
            {
                return new BaseApiResInfo<string>
                {
                    Status = false,
                    StatusCode = 500,
                    Msg = ex.Message,
                    Timestamp = DateTime.Now.ToString("yyyyMMddHHmmss")
                };
            }
        }
    }
}
```

---

## 注意事项

1. **Controller命名**：Controller名称应具有明确的业务含义
2. **Action命名**：Action名称应清晰表达业务用途
3. **参数解析**：使用 `FastJsonSerializer.FromJson` 解析Args参数
4. **异常处理**：统一使用 try-catch 捕获异常并返回错误信息
5. **平台类型**：Platform=0表示非跨平台，1表示跨平台券库

---

## ⚠️ 生成后自检清单

生成代码后必须检查以下项：

| 检查项 | 要求 | 通过条件 |
|--------|------|----------|
| Controller继承 | 必须继承 `ControllerBase` | ✓ 不是 `BaseApiController` |
| 业务方法类型 | 必须是 `public static` | ✓ 不是依赖注入 |
| 方法名 | 必须为 `CoupGrpDisable` | ✓ 与示例一致 |
| 代码标记 | 必须包含标记 | ✓ summary首行有 `[EZR-API:1312]` |
| 调用方式 | 必须是静态方法调用 | ✓ `CoupGrpManager.CoupGrpDisable(...)` |

---

## 联系支持

如有问题，请联系 EZR 技术支持团队。